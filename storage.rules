rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read and write all files.
    // This resolves the 403 Forbidden and preflight CORS errors for logo uploads.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
