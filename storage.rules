rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Tightened rules to require authentication now that connectivity is confirmed.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
