rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read and write all files.
    // Synchronized with root rules to ensure consistent behavior.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
