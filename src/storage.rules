rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Synchronized with root rules for consistency
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
